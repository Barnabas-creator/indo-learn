// 加密内容分几份，这里是唯一的清单。
//
// 加一个内容模块时只改这一行，其余三处（打包脚本、两个 provider 的取数出口）
// 都从这里读——以前是各写各的，加一个模块要在三个文件里各加一行，很容易漏。
// 视图层、首页卡片、校验器仍要各自加，那些本来就是每个模块不一样的东西。
export const CONTENT_MODULES = ['packs', 'dialogs', 'grammar', 'course', 'roots'];
