# 解包所得的参考资料，仅供开发时查阅，不发布
reference/

# 打包脚本的明文输入 — 加密前的内容原文绝不提交
content-src/
*.plain.json

# 审校表含全部词条与对话的明文，提交到公开仓库会绕过加密
# 后缀一概不限：xlsx / html / csv 都是明文
*审校*
*review*.xlsx
*review*.html

# 密码/密钥
.env
*.secret

node_modules/
.DS_Store
scratchpad-*.json

# wrangler 的本机缓存（含 account id），不进仓库
.wrangler/
