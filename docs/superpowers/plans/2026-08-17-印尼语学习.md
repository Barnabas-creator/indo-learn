# 印尼语学习 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建一个纯前端离线 PWA「印尼语学习」，含单词包、场景对话、语法学习三个模块，内容经 AES-GCM 加密后由密码解锁，部署到 GitHub Pages。

**Architecture:** 无构建步骤的原生 ES Module 应用。外壳代码（HTML/JS/CSS）与内容数据（加密的 `.enc` 文件）完全分离；所有内容访问经由单一 `provider` 接口，为将来切换服务器留出替换点。`lib/crypto.js` 使用 Web Crypto API，同一份代码在浏览器与 Node 打包脚本中共用。

**Tech Stack:** 原生 ES Modules、Web Crypto API（AES-GCM / PBKDF2）、Web Speech API（`speechSynthesis`）、Node 22 内置 `node:test`、OpenMoji 17.0.0 SVG（CC-BY-SA-4.0）、Service Worker、GitHub Pages。

## Global Constraints

- **无构建步骤。** 浏览器直接加载 ES Module，不引入 webpack/vite/babel。
- **无运行时第三方依赖。** `package.json` 的 `dependencies` 必须为空；开发期工具也不引入生产依赖。
- **绝不记录学习进度。** 不得写入任何 `x/10`、"已完成"、"解锁"、`localStorage` 进度状态。`localStorage` 仅允许存 `{ cek, expiresAt, contentVersion }` 一个解锁凭据键。
- **解密后的明文只留在内存。** 禁止把解密结果写入 `localStorage`、`IndexedDB`、`sessionStorage` 或 Cache Storage。
- **PBKDF2 迭代次数固定 250000，哈希 SHA-256。** salt 16 字节随机。
- **AES-GCM，IV 12 字节随机，每个文件独立 IV。**
- **解锁有效期常量 `UNLOCK_TTL_DAYS = 30`**，定义在 `lib/provider.js`。
- **Service Worker 缓存策略：** 外壳与 `data/<version>/*.enc` 用 cache-first；`data/manifest.json` 与 `data/keys.json` 用 network-first。
- **不提交明文内容。** `content-src/` 与 `reference/` 已在 `.gitignore` 中，不得移除。
- **不得在仓库任何位置写入真实密码。** 打包脚本从命令行参数或环境变量读取。
- **界面语言为简体中文**，学习内容为印尼语 + 中文对照。
- **Git 身份：** `barnabas7223 <barnabas7223@gmail.com>`（仓库已配置）。
- **提交信息用中文，类型前缀用英文**（`feat:` / `fix:` / `docs:` / `chore:`）。

---

## File Structure

| 路径 | 职责 |
|---|---|
| `index.html` | 外壳骨架，挂载点，注册 Service Worker |
| `styles.css` | 全部样式 |
| `app.js` | 路由与页面切换，串联各视图 |
| `lib/crypto.js` | AES-GCM / PBKDF2 原语，浏览器与 Node 共用 |
| `lib/provider.js` | `StaticEncryptedProvider`：解锁、取内容、管理解锁凭据 |
| `lib/tts.js` | `speechSynthesis` 封装，`id-ID` 音色检测 |
| `lib/icons.js` | 词 → OpenMoji 码位映射查询，兜底主题图标 |
| `lib/views/packs.js` | 单词包列表 + 词卡视图 |
| `lib/views/dialogs.js` | 场景对话视图 |
| `lib/views/grammar.js` | 语法学习视图 |
| `lib/views/unlock.js` | 密码解锁界面 |
| `tools/extract-packs.mjs` | 从解包结果提取 100 个初级包骨架 |
| `tools/extract-grammar.mjs` | 从解包结果提取语法课程 JSON |
| `tools/fetch-openmoji.mjs` | 按映射表从 jsdelivr 拉取所需 SVG |
| `tools/pack-content.mjs` | 加密打包：生成 `keys.json`、`manifest.json`、`data/<v>/*.enc` |
| `tools/*.test.mjs` | `node:test` 单元测试 |
| `content-src/*.json` | 明文内容（gitignored） |
| `data/` | 加密产物 + manifest + keys |
| `assets/openmoji/` | 用到的 OpenMoji SVG 子集 |
| `sw.js` | Service Worker |
| `manifest.webmanifest` | PWA 清单 |

---

### Task 1: 加密原语 `lib/crypto.js`

**Files:**
- Create: `lib/crypto.js`
- Test: `tools/crypto.test.mjs`

**Interfaces:**
- Consumes: 无
- Produces:
  - `generateCek(): Promise<CryptoKey>` — 生成 256 位 AES-GCM 密钥，`extractable: true`
  - `deriveKek(password: string, saltB64: string, iterations: number): Promise<CryptoKey>`
  - `wrapCek(cek: CryptoKey, kek: CryptoKey): Promise<{ iv: string, wrapped: string }>` — 值为 base64
  - `unwrapCek(wrappedB64: string, ivB64: string, kek: CryptoKey): Promise<CryptoKey>` — 密码错误时抛出
  - `encryptJson(obj: unknown, cek: CryptoKey): Promise<{ iv: string, data: string }>` — base64
  - `decryptJson(dataB64: string, ivB64: string, cek: CryptoKey): Promise<unknown>`
  - `randomB64(bytes: number): string`
  - `exportCek(cek: CryptoKey): Promise<string>` / `importCek(b64: string): Promise<CryptoKey>`

- [ ] **Step 1: 写失败的测试**

```js
// tools/crypto.test.mjs
import { test } from 'node:test';
import assert from 'node:assert/strict';
import {
  generateCek, deriveKek, wrapCek, unwrapCek,
  encryptJson, decryptJson, randomB64, exportCek, importCek,
} from '../lib/crypto.js';

test('往返加解密还原原对象', async () => {
  const cek = await generateCek();
  const payload = { packs: [{ id: 'p1', words: ['satu', 'dua'] }] };
  const { iv, data } = await encryptJson(payload, cek);
  assert.deepEqual(await decryptJson(data, iv, cek), payload);
});

test('每次加密的 IV 都不同', async () => {
  const cek = await generateCek();
  const a = await encryptJson({ x: 1 }, cek);
  const b = await encryptJson({ x: 1 }, cek);
  assert.notEqual(a.iv, b.iv);
  assert.notEqual(a.data, b.data);
});

test('正确密码可以解出 CEK，且能解密内容', async () => {
  const cek = await generateCek();
  const salt = randomB64(16);
  const kek = await deriveKek('正确密码', salt, 250000);
  const { iv, wrapped } = await wrapCek(cek, kek);

  const kek2 = await deriveKek('正确密码', salt, 250000);
  const cek2 = await unwrapCek(wrapped, iv, kek2);

  const enc = await encryptJson({ ok: true }, cek);
  assert.deepEqual(await decryptJson(enc.data, enc.iv, cek2), { ok: true });
});

test('错误密码解 CEK 时抛异常', async () => {
  const cek = await generateCek();
  const salt = randomB64(16);
  const kek = await deriveKek('正确密码', salt, 250000);
  const { iv, wrapped } = await wrapCek(cek, kek);

  const bad = await deriveKek('错误密码', salt, 250000);
  await assert.rejects(() => unwrapCek(wrapped, iv, bad));
});

test('CEK 可导出再导入，仍能解密', async () => {
  const cek = await generateCek();
  const enc = await encryptJson({ v: 42 }, cek);
  const restored = await importCek(await exportCek(cek));
  assert.deepEqual(await decryptJson(enc.data, enc.iv, restored), { v: 42 });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd ~/印尼语学习 && node --test tools/crypto.test.mjs`
Expected: FAIL — `Cannot find module '../lib/crypto.js'`

- [ ] **Step 3: 实现**

```js
// lib/crypto.js
// AES-GCM / PBKDF2 原语。同一份代码在浏览器和 Node 打包脚本中共用。

const subtle = globalThis.crypto.subtle;
const enc = new TextEncoder();
const dec = new TextDecoder();

const IV_BYTES = 12;

function toB64(buf) {
  const bytes = new Uint8Array(buf);
  let s = '';
  for (const b of bytes) s += String.fromCharCode(b);
  return btoa(s);
}

function fromB64(b64) {
  const s = atob(b64);
  const bytes = new Uint8Array(s.length);
  for (let i = 0; i < s.length; i++) bytes[i] = s.charCodeAt(i);
  return bytes;
}

export function randomB64(bytes) {
  return toB64(globalThis.crypto.getRandomValues(new Uint8Array(bytes)));
}

export function generateCek() {
  return subtle.generateKey({ name: 'AES-GCM', length: 256 }, true, [
    'encrypt',
    'decrypt',
  ]);
}

export async function exportCek(cek) {
  return toB64(await subtle.exportKey('raw', cek));
}

export function importCek(b64) {
  return subtle.importKey('raw', fromB64(b64), { name: 'AES-GCM' }, true, [
    'encrypt',
    'decrypt',
  ]);
}

export async function deriveKek(password, saltB64, iterations) {
  const base = await subtle.importKey(
    'raw',
    enc.encode(password),
    'PBKDF2',
    false,
    ['deriveKey'],
  );
  return subtle.deriveKey(
    {
      name: 'PBKDF2',
      salt: fromB64(saltB64),
      iterations,
      hash: 'SHA-256',
    },
    base,
    { name: 'AES-GCM', length: 256 },
    false,
    ['wrapKey', 'unwrapKey'],
  );
}

export async function wrapCek(cek, kek) {
  const iv = globalThis.crypto.getRandomValues(new Uint8Array(IV_BYTES));
  const wrapped = await subtle.wrapKey('raw', cek, kek, {
    name: 'AES-GCM',
    iv,
  });
  return { iv: toB64(iv), wrapped: toB64(wrapped) };
}

export function unwrapCek(wrappedB64, ivB64, kek) {
  return subtle.unwrapKey(
    'raw',
    fromB64(wrappedB64),
    kek,
    { name: 'AES-GCM', iv: fromB64(ivB64) },
    { name: 'AES-GCM' },
    true,
    ['encrypt', 'decrypt'],
  );
}

export async function encryptJson(obj, cek) {
  const iv = globalThis.crypto.getRandomValues(new Uint8Array(IV_BYTES));
  const data = await subtle.encrypt(
    { name: 'AES-GCM', iv },
    cek,
    enc.encode(JSON.stringify(obj)),
  );
  return { iv: toB64(iv), data: toB64(data) };
}

export async function decryptJson(dataB64, ivB64, cek) {
  const plain = await subtle.decrypt(
    { name: 'AES-GCM', iv: fromB64(ivB64) },
    cek,
    fromB64(dataB64),
  );
  return JSON.parse(dec.decode(plain));
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd ~/印尼语学习 && node --test tools/crypto.test.mjs`
Expected: PASS，5 个测试全绿

- [ ] **Step 5: 提交**

```bash
cd ~/印尼语学习
git add lib/crypto.js tools/crypto.test.mjs
git commit -m "feat: 加密原语 AES-GCM + PBKDF2"
```

---

### Task 2: 提取单词包骨架 `tools/extract-packs.mjs`

从 `reference/packs.js` 提取初级 100 个包的标题、副标题、主题，输出骨架 JSON 供后续填词。

**Files:**
- Create: `tools/extract-packs.mjs`
- Test: `tools/extract-packs.test.mjs`
- Output: `content-src/pack-skeleton.json`（gitignored）

**Interfaces:**
- Consumes: 无
- Produces:
  - `parsePackSkeleton(source: string): Array<{ id, title, subtitle, theme, stage }>` — 从 `packs.js` 源码文本解析，只返回 `level === 'beginner'` 的条目，按 `stage` 升序

- [ ] **Step 1: 写失败的测试**

```js
// tools/extract-packs.test.mjs
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { parsePackSkeleton } from './extract-packs.mjs';

const SAMPLE = `
  wordPackCards: [
    { id: "freq-beginner-002", title: "饮料", subtitle: "水茶咖啡", total: 10,
      added: 0, icon: "/images/home/icon-pack-daily.svg", theme: "blue",
      category: "beginner", level: "beginner", stage: 2 },
    { id: "freq-beginner-001", title: "数字", subtitle: "1到10", total: 10,
      added: 0, icon: "/images/home/icon-pack-daily.svg", theme: "blue",
      category: "beginner", level: "beginner", stage: 1 },
    { id: "freq-inter-001", title: "人际", subtitle: "亲友关系", total: 10,
      added: 0, icon: "/images/home/icon-pack-daily.svg", theme: "blue",
      category: "intermediate", level: "intermediate", stage: 1 },
  ],
`;

test('只提取初级包', () => {
  const out = parsePackSkeleton(SAMPLE);
  assert.equal(out.length, 2);
  assert.ok(out.every((p) => p.id.startsWith('freq-beginner-')));
});

test('按 stage 升序排列', () => {
  const out = parsePackSkeleton(SAMPLE);
  assert.deepEqual(out.map((p) => p.stage), [1, 2]);
  assert.equal(out[0].title, '数字');
  assert.equal(out[0].subtitle, '1到10');
});

test('保留 theme 字段', () => {
  const out = parsePackSkeleton(SAMPLE);
  assert.equal(out[0].theme, 'blue');
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd ~/印尼语学习 && node --test tools/extract-packs.test.mjs`
Expected: FAIL — 模块不存在

- [ ] **Step 3: 实现**

```js
// tools/extract-packs.mjs
// 从小程序解包所得的 packs.js 中提取初级 100 个单词包的骨架。
import { readFileSync, writeFileSync, mkdirSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { dirname, join } from 'node:path';

const FIELD = String.raw`id: "([^"]+)",\s*title: "([^"]+)",\s*subtitle: "([^"]*)",\s*total: (\d+),\s*added: \d+,\s*icon: "[^"]*",\s*theme: "([^"]*)",\s*category: "[^"]*",\s*level: "([^"]*)",\s*stage: (\d+)`;

export function parsePackSkeleton(source) {
  const re = new RegExp(FIELD, 'g');
  const out = [];
  for (const m of source.matchAll(re)) {
    const [, id, title, subtitle, total, theme, level, stage] = m;
    if (level !== 'beginner') continue;
    out.push({
      id,
      title,
      subtitle,
      theme,
      stage: Number(stage),
      total: Number(total),
      words: [],
    });
  }
  out.sort((a, b) => a.stage - b.stage);
  return out;
}

const isMain = process.argv[1] === fileURLToPath(import.meta.url);
if (isMain) {
  const root = join(dirname(fileURLToPath(import.meta.url)), '..');
  const src = readFileSync(join(root, 'reference/packs.js'), 'utf8');
  const packs = parsePackSkeleton(src);
  mkdirSync(join(root, 'content-src'), { recursive: true });
  writeFileSync(
    join(root, 'content-src/pack-skeleton.json'),
    JSON.stringify(packs, null, 2),
  );
  console.log(`提取 ${packs.length} 个初级包 -> content-src/pack-skeleton.json`);
}
```

- [ ] **Step 4: 运行测试确认通过，再跑一次真实提取**

Run: `cd ~/印尼语学习 && node --test tools/extract-packs.test.mjs && node tools/extract-packs.mjs`
Expected: 测试 PASS；命令输出 `提取 100 个初级包`

- [ ] **Step 5: 提交**

```bash
cd ~/印尼语学习
git add tools/extract-packs.mjs tools/extract-packs.test.mjs
git commit -m "feat: 提取初级单词包骨架"
```

---

### Task 3: 提取语法课程 `tools/extract-grammar.mjs`

`reference/curriculum.js` 是被压缩过的模块，直接正则解析不可靠。做法是把它当模块求值：剥掉小程序的 `define(...)` 包装，用 `node:vm` 在沙箱中执行，取 `module.exports`。

**Files:**
- Create: `tools/extract-grammar.mjs`
- Test: `tools/extract-grammar.test.mjs`
- Output: `content-src/grammar.json`（gitignored）、`assets/grammar-svg/`

**Interfaces:**
- Consumes: 无
- Produces:
  - `evalDefineModule(source: string, moduleName: string): unknown` — 求值小程序 `define()` 包装的模块，返回其 `module.exports`
  - `normalizeGrammar(raw: unknown): Array<{ id, number, title, subtitle, visual, lessons }>`

- [ ] **Step 1: 写失败的测试**

```js
// tools/extract-grammar.test.mjs
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { evalDefineModule, normalizeGrammar } from './extract-grammar.mjs';

const SAMPLE = `define("pages/grammar/curriculum.js", function (require, module, exports) {
  "use strict";
  var mk = function (label, result, meaning, example, translation, note) {
    return { label: label, result: result, meaning: meaning,
             example: example, translation: translation, note: note || "" };
  };
  module.exports = {
    modules: [{
      id: "word-family", number: "01", title: "词根与词缀",
      subtitle: "一个词根，变出动作、人物、结果和状态", visual: "morph",
      lessons: [{
        id: "lihat-family", title: "lihat：看见家族", kind: "morph",
        base: "lihat · 看", instruction: "选择词缀。",
        options: [ mk("meN-", "melihat", "看；看见",
                      "Saya melihat mobil itu.", "我看见那辆车。",
                      "meN- 遇到 l 表现为 me-。") ],
      }],
    }],
  };
}, { isPage: false });`;

test('能求值 define 包装的模块', () => {
  const mod = evalDefineModule(SAMPLE, 'pages/grammar/curriculum.js');
  assert.ok(Array.isArray(mod.modules));
  assert.equal(mod.modules[0].id, 'word-family');
});

test('规范化后保留课程与选项的完整字段', () => {
  const mod = evalDefineModule(SAMPLE, 'pages/grammar/curriculum.js');
  const out = normalizeGrammar(mod);
  assert.equal(out.length, 1);
  const lesson = out[0].lessons[0];
  assert.equal(lesson.base, 'lihat · 看');
  const opt = lesson.options[0];
  assert.deepEqual(opt, {
    label: 'meN-',
    result: 'melihat',
    meaning: '看；看见',
    example: 'Saya melihat mobil itu.',
    translation: '我看见那辆车。',
    note: 'meN- 遇到 l 表现为 me-。',
  });
});

test('缺 note 的选项补空字符串', () => {
  const src = SAMPLE.replace(
    '"meN- 遇到 l 表现为 me-。"',
    'undefined',
  );
  const out = normalizeGrammar(evalDefineModule(src, 'pages/grammar/curriculum.js'));
  assert.equal(out[0].lessons[0].options[0].note, '');
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd ~/印尼语学习 && node --test tools/extract-grammar.test.mjs`
Expected: FAIL — 模块不存在

- [ ] **Step 3: 实现**

```js
// tools/extract-grammar.mjs
// 从小程序解包结果提取语法课程。curriculum.js 是压缩过的 define() 模块，
// 直接正则解析不可靠，改为在 vm 沙箱中求值取 module.exports。
import { readFileSync, writeFileSync, mkdirSync, cpSync, existsSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { dirname, join } from 'node:path';
import vm from 'node:vm';

export function evalDefineModule(source, moduleName) {
  let captured = null;
  const sandbox = {
    define(name, factory) {
      if (name !== moduleName) return;
      const module = { exports: {} };
      factory(
        () => ({}),          // require —— 语法数据模块不依赖其他模块
        module,
        module.exports,
      );
      captured = module.exports;
    },
    require: () => ({}),
    console,
  };
  vm.createContext(sandbox);
  vm.runInContext(source, sandbox, { timeout: 5000 });
  if (!captured) throw new Error(`未找到模块 ${moduleName}`);
  return captured;
}

function normalizeOption(o) {
  return {
    label: o.label ?? '',
    result: o.result ?? '',
    meaning: o.meaning ?? '',
    example: o.example ?? '',
    translation: o.translation ?? '',
    note: o.note ?? '',
  };
}

export function normalizeGrammar(raw) {
  const modules = raw?.modules ?? raw ?? [];
  return modules.map((m) => ({
    id: m.id,
    number: m.number ?? '',
    title: m.title ?? '',
    subtitle: m.subtitle ?? '',
    visual: m.visual ?? '',
    lessons: (m.lessons ?? []).map((l) => ({
      id: l.id,
      title: l.title ?? '',
      base: l.base ?? '',
      instruction: l.instruction ?? '',
      tip: l.tip ?? '',
      options: (l.options ?? []).map(normalizeOption),
    })),
  }));
}

const isMain = process.argv[1] === fileURLToPath(import.meta.url);
if (isMain) {
  const root = join(dirname(fileURLToPath(import.meta.url)), '..');
  const src = readFileSync(join(root, 'reference/curriculum.js'), 'utf8');
  const grammar = normalizeGrammar(
    evalDefineModule(src, 'pages/grammar/curriculum.js'),
  );
  mkdirSync(join(root, 'content-src'), { recursive: true });
  writeFileSync(
    join(root, 'content-src/grammar.json'),
    JSON.stringify(grammar, null, 2),
  );

  const svgSrc = join(root, 'reference/wxapkg-unpacked/grammar/pages/grammar/images');
  if (existsSync(svgSrc)) {
    cpSync(svgSrc, join(root, 'assets/grammar-svg'), { recursive: true });
  }

  const lessons = grammar.reduce((n, m) => n + m.lessons.length, 0);
  console.log(`提取 ${grammar.length} 个语法模块 / ${lessons} 课 -> content-src/grammar.json`);
}
```

- [ ] **Step 4: 运行测试确认通过，再跑真实提取**

Run: `cd ~/印尼语学习 && node --test tools/extract-grammar.test.mjs && node tools/extract-grammar.mjs`
Expected: 测试 PASS；输出提取到的模块数与课数（非零）

- [ ] **Step 5: 提交**

```bash
cd ~/印尼语学习
git add tools/extract-grammar.mjs tools/extract-grammar.test.mjs assets/grammar-svg
git commit -m "feat: 提取语法课程与插图"
```

---

### Task 4: 打包加密 `tools/pack-content.mjs`

**Files:**
- Create: `tools/pack-content.mjs`
- Test: `tools/pack-content.test.mjs`
- Output: `data/manifest.json`、`data/keys.json`、`data/<version>/*.enc`

**Interfaces:**
- Consumes: `lib/crypto.js` 的全部导出
- Produces:
  - `buildBundle(content: { packs, dialogs, grammar }, password: string, version: string): Promise<{ manifest, keys, files: Record<string, { iv, data }> }>`
  - `PBKDF2_ITERATIONS = 250000`（导出常量）

- [ ] **Step 1: 写失败的测试**

```js
// tools/pack-content.test.mjs
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { buildBundle, PBKDF2_ITERATIONS } from './pack-content.mjs';
import { deriveKek, unwrapCek, decryptJson } from '../lib/crypto.js';

const CONTENT = {
  packs: [{ id: 'p1', title: '数字', subtitle: '1到10', theme: 'blue', words: [] }],
  dialogs: [{ id: 'd1', scene: 'Sapaan', sceneZh: '打招呼', lines: [] }],
  grammar: [{ id: 'g1', title: '词根与词缀', lessons: [] }],
};

test('产出 manifest 指向给定版本', async () => {
  const b = await buildBundle(CONTENT, '密码A', 'v1');
  assert.equal(b.manifest.contentVersion, 'v1');
});

test('keys.json 带 salt 与固定迭代次数', async () => {
  const b = await buildBundle(CONTENT, '密码A', 'v1');
  assert.equal(b.keys.kdf.iterations, PBKDF2_ITERATIONS);
  assert.ok(b.keys.kdf.salt.length > 0);
});

test('三个内容文件都被加密', async () => {
  const b = await buildBundle(CONTENT, '密码A', 'v1');
  assert.deepEqual(Object.keys(b.files).sort(), [
    'dialogs.enc', 'grammar.enc', 'packs.enc',
  ]);
});

test('用正确密码可还原内容', async () => {
  const b = await buildBundle(CONTENT, '密码A', 'v1');
  const kek = await deriveKek('密码A', b.keys.kdf.salt, b.keys.kdf.iterations);
  const cek = await unwrapCek(b.keys.wrapped, b.keys.iv, kek);
  const packs = await decryptJson(b.files['packs.enc'].data, b.files['packs.enc'].iv, cek);
  assert.deepEqual(packs, CONTENT.packs);
});

test('错误密码无法还原', async () => {
  const b = await buildBundle(CONTENT, '密码A', 'v1');
  const kek = await deriveKek('密码B', b.keys.kdf.salt, b.keys.kdf.iterations);
  await assert.rejects(() => unwrapCek(b.keys.wrapped, b.keys.iv, kek));
});

test('换密码时同时换 CEK —— 旧密码解不开新包', async () => {
  const v1 = await buildBundle(CONTENT, '密码A', 'v1');
  const v2 = await buildBundle(CONTENT, '密码B', 'v2');

  // 用旧密码解出旧 CEK
  const oldKek = await deriveKek('密码A', v1.keys.kdf.salt, v1.keys.kdf.iterations);
  const oldCek = await unwrapCek(v1.keys.wrapped, v1.keys.iv, oldKek);

  // 旧 CEK 不能解开新版本的内容
  await assert.rejects(() =>
    decryptJson(v2.files['packs.enc'].data, v2.files['packs.enc'].iv, oldCek),
  );
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd ~/印尼语学习 && node --test tools/pack-content.test.mjs`
Expected: FAIL — 模块不存在

- [ ] **Step 3: 实现**

```js
// tools/pack-content.mjs
// 把明文内容加密打包成 data/<version>/*.enc + keys.json + manifest.json。
//
// 用法：
//   node tools/pack-content.mjs --password '你的密码' --version v1
//
// 换密码时必须同时提高版本号：本脚本每次都生成全新的 CEK 并重新加密全部内容。
// 只换 keys.json 而复用 CEK 是无效的 —— 保留旧 keys.json 的人用旧密码
// 仍能解出同一个 CEK，进而解开新数据。
import { readFileSync, writeFileSync, mkdirSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { dirname, join } from 'node:path';
import {
  generateCek, deriveKek, wrapCek, encryptJson, randomB64,
} from '../lib/crypto.js';

export const PBKDF2_ITERATIONS = 250000;

export async function buildBundle(content, password, version) {
  const cek = await generateCek();
  const salt = randomB64(16);
  const kek = await deriveKek(password, salt, PBKDF2_ITERATIONS);
  const { iv, wrapped } = await wrapCek(cek, kek);

  const files = {};
  for (const [name, value] of [
    ['packs.enc', content.packs],
    ['dialogs.enc', content.dialogs],
    ['grammar.enc', content.grammar],
  ]) {
    files[name] = await encryptJson(value, cek);
  }

  return {
    manifest: { contentVersion: version, builtAt: new Date().toISOString() },
    keys: {
      version,
      kdf: { name: 'PBKDF2', hash: 'SHA-256', salt, iterations: PBKDF2_ITERATIONS },
      iv,
      wrapped,
    },
    files,
  };
}

function arg(name, fallback) {
  const i = process.argv.indexOf(`--${name}`);
  if (i >= 0 && process.argv[i + 1]) return process.argv[i + 1];
  return fallback;
}

const isMain = process.argv[1] === fileURLToPath(import.meta.url);
if (isMain) {
  const root = join(dirname(fileURLToPath(import.meta.url)), '..');
  const password = arg('password', process.env.CONTENT_PASSWORD);
  const version = arg('version', 'v1');

  if (!password) {
    console.error('缺少密码。用法：node tools/pack-content.mjs --password <密码> --version v1');
    process.exit(1);
  }

  const read = (f) =>
    JSON.parse(readFileSync(join(root, 'content-src', f), 'utf8'));
  const bundle = await buildBundle(
    { packs: read('packs.json'), dialogs: read('dialogs.json'), grammar: read('grammar.json') },
    password,
    version,
  );

  mkdirSync(join(root, 'data', version), { recursive: true });
  writeFileSync(join(root, 'data/manifest.json'), JSON.stringify(bundle.manifest, null, 2));
  writeFileSync(join(root, 'data/keys.json'), JSON.stringify(bundle.keys, null, 2));
  for (const [name, payload] of Object.entries(bundle.files)) {
    writeFileSync(join(root, 'data', version, name), JSON.stringify(payload));
  }
  console.log(`打包完成 -> data/${version}/（packs, dialogs, grammar）`);
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd ~/印尼语学习 && node --test tools/pack-content.test.mjs`
Expected: PASS，6 个测试全绿

- [ ] **Step 5: 提交**

```bash
cd ~/印尼语学习
git add tools/pack-content.mjs tools/pack-content.test.mjs
git commit -m "feat: 内容加密打包工具链"
```

---

### Task 5: 内容访问接口 `lib/provider.js`

**Files:**
- Create: `lib/provider.js`
- Test: `tools/provider.test.mjs`

**Interfaces:**
- Consumes: `lib/crypto.js`
- Produces:
  - `UNLOCK_TTL_DAYS = 30`
  - `STORAGE_KEY = 'indo-learn-unlock'`
  - `createProvider({ fetchJson, storage, now }): Provider`（依赖注入，便于测试）
  - Provider 方法：
    - `init(): Promise<{ unlocked: boolean }>` — 读 manifest 与 keys，检查已存凭据是否仍有效
    - `unlock(password: string): Promise<void>` — 失败时抛 `Error('密码不正确')`
    - `getPacks(): Promise<Array>`、`getDialogs(): Promise<Array>`、`getGrammar(): Promise<Array>`
    - `lock(): void` — 清除凭据与内存明文

- [ ] **Step 1: 写失败的测试**

```js
// tools/provider.test.mjs
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { createProvider, UNLOCK_TTL_DAYS, STORAGE_KEY } from '../lib/provider.js';
import { buildBundle } from './pack-content.mjs';

const CONTENT = {
  packs: [{ id: 'p1', title: '数字', words: [] }],
  dialogs: [{ id: 'd1', sceneZh: '打招呼', lines: [] }],
  grammar: [{ id: 'g1', title: '词缀', lessons: [] }],
};

function makeStorage() {
  const map = new Map();
  return {
    getItem: (k) => (map.has(k) ? map.get(k) : null),
    setItem: (k, v) => map.set(k, v),
    removeItem: (k) => map.delete(k),
    _map: map,
  };
}

async function harness({ password = '密码A', version = 'v1', now = () => 1_000_000 } = {}) {
  const bundle = await buildBundle(CONTENT, password, version);
  const routes = {
    'data/manifest.json': bundle.manifest,
    'data/keys.json': bundle.keys,
    [`data/${version}/packs.enc`]: bundle.files['packs.enc'],
    [`data/${version}/dialogs.enc`]: bundle.files['dialogs.enc'],
    [`data/${version}/grammar.enc`]: bundle.files['grammar.enc'],
  };
  const storage = makeStorage();
  const provider = createProvider({
    fetchJson: async (path) => {
      if (!(path in routes)) throw new Error(`404 ${path}`);
      return routes[path];
    },
    storage,
    now,
  });
  return { provider, storage, bundle };
}

test('初始状态未解锁', async () => {
  const { provider } = await harness();
  assert.deepEqual(await provider.init(), { unlocked: false });
});

test('正确密码解锁后可取内容', async () => {
  const { provider } = await harness();
  await provider.init();
  await provider.unlock('密码A');
  assert.deepEqual(await provider.getPacks(), CONTENT.packs);
  assert.deepEqual(await provider.getDialogs(), CONTENT.dialogs);
  assert.deepEqual(await provider.getGrammar(), CONTENT.grammar);
});

test('错误密码抛出「密码不正确」', async () => {
  const { provider } = await harness();
  await provider.init();
  await assert.rejects(() => provider.unlock('错误'), /密码不正确/);
});

test('解锁后写入带有效期的凭据', async () => {
  const now = () => 1_000_000;
  const { provider, storage } = await harness({ now });
  await provider.init();
  await provider.unlock('密码A');
  const saved = JSON.parse(storage.getItem(STORAGE_KEY));
  assert.equal(saved.contentVersion, 'v1');
  assert.equal(saved.expiresAt, 1_000_000 + UNLOCK_TTL_DAYS * 86400_000);
  assert.ok(saved.cek);
});

test('凭据未过期时再次 init 直接解锁', async () => {
  let t = 1_000_000;
  const { provider, storage, bundle } = await harness({ now: () => t });
  await provider.init();
  await provider.unlock('密码A');
  const saved = storage.getItem(STORAGE_KEY);

  t += 86400_000; // 过一天
  const p2 = createProvider({
    fetchJson: async (path) => ({
      'data/manifest.json': bundle.manifest,
      'data/keys.json': bundle.keys,
      'data/v1/packs.enc': bundle.files['packs.enc'],
      'data/v1/dialogs.enc': bundle.files['dialogs.enc'],
      'data/v1/grammar.enc': bundle.files['grammar.enc'],
    })[path],
    storage: { getItem: () => saved, setItem: () => {}, removeItem: () => {} },
    now: () => t,
  });
  assert.deepEqual(await p2.init(), { unlocked: true });
  assert.deepEqual(await p2.getPacks(), CONTENT.packs);
});

test('凭据过期后回到未解锁', async () => {
  let t = 1_000_000;
  const { provider, storage, bundle } = await harness({ now: () => t });
  await provider.init();
  await provider.unlock('密码A');
  const saved = storage.getItem(STORAGE_KEY);

  t += (UNLOCK_TTL_DAYS + 1) * 86400_000;
  const p2 = createProvider({
    fetchJson: async (path) => ({
      'data/manifest.json': bundle.manifest,
      'data/keys.json': bundle.keys,
    })[path],
    storage: { getItem: () => saved, setItem: () => {}, removeItem: () => {} },
    now: () => t,
  });
  assert.deepEqual(await p2.init(), { unlocked: false });
});

test('内容版本变化时旧凭据失效', async () => {
  const t = 1_000_000;
  const { provider, storage } = await harness({ now: () => t });
  await provider.init();
  await provider.unlock('密码A');
  const saved = storage.getItem(STORAGE_KEY);

  const v2 = await buildBundle(CONTENT, '密码B', 'v2');
  const p2 = createProvider({
    fetchJson: async (path) => ({
      'data/manifest.json': v2.manifest,
      'data/keys.json': v2.keys,
    })[path],
    storage: { getItem: () => saved, setItem: () => {}, removeItem: () => {} },
    now: () => t,
  });
  assert.deepEqual(await p2.init(), { unlocked: false });
});

test('lock 后无法再取内容', async () => {
  const { provider } = await harness();
  await provider.init();
  await provider.unlock('密码A');
  provider.lock();
  await assert.rejects(() => provider.getPacks(), /未解锁/);
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd ~/印尼语学习 && node --test tools/provider.test.mjs`
Expected: FAIL — 模块不存在

- [ ] **Step 3: 实现**

```js
// lib/provider.js
// 内容访问接口。今天只有静态加密实现；将来上服务器时新增 RemoteProvider，
// 保持同样的方法签名，UI 层不改动。
//
// 明文只保留在内存（闭包变量），绝不写入任何持久化存储 ——
// 一旦落盘，密码轮换即失去意义。
import { deriveKek, unwrapCek, decryptJson, exportCek, importCek } from './crypto.js';

export const UNLOCK_TTL_DAYS = 30;
export const STORAGE_KEY = 'indo-learn-unlock';

const DAY_MS = 86400_000;

export function createProvider({ fetchJson, storage, now = () => Date.now() }) {
  let manifest = null;
  let keys = null;
  let cek = null;
  const cache = new Map(); // 内存明文缓存，随页面关闭消失

  function saveCredential(cekB64) {
    storage.setItem(
      STORAGE_KEY,
      JSON.stringify({
        cek: cekB64,
        contentVersion: manifest.contentVersion,
        keysVersion: keys.version,
        expiresAt: now() + UNLOCK_TTL_DAYS * DAY_MS,
      }),
    );
  }

  function readCredential() {
    const raw = storage.getItem(STORAGE_KEY);
    if (!raw) return null;
    try {
      return JSON.parse(raw);
    } catch {
      return null;
    }
  }

  async function init() {
    manifest = await fetchJson('data/manifest.json');
    keys = await fetchJson('data/keys.json');

    const saved = readCredential();
    const valid =
      saved &&
      saved.expiresAt > now() &&
      saved.contentVersion === manifest.contentVersion &&
      saved.keysVersion === keys.version;

    if (!valid) {
      storage.removeItem(STORAGE_KEY);
      cek = null;
      return { unlocked: false };
    }

    cek = await importCek(saved.cek);
    return { unlocked: true };
  }

  async function unlock(password) {
    const kek = await deriveKek(password, keys.kdf.salt, keys.kdf.iterations);
    try {
      cek = await unwrapCek(keys.wrapped, keys.iv, kek);
    } catch {
      throw new Error('密码不正确');
    }
    saveCredential(await exportCek(cek));
  }

  async function load(name) {
    if (!cek) throw new Error('未解锁');
    if (cache.has(name)) return cache.get(name);
    const payload = await fetchJson(`data/${manifest.contentVersion}/${name}.enc`);
    const value = await decryptJson(payload.data, payload.iv, cek);
    cache.set(name, value);
    return value;
  }

  function lock() {
    cek = null;
    cache.clear();
    storage.removeItem(STORAGE_KEY);
  }

  return {
    init,
    unlock,
    lock,
    getPacks: () => load('packs'),
    getDialogs: () => load('dialogs'),
    getGrammar: () => load('grammar'),
  };
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd ~/印尼语学习 && node --test tools/provider.test.mjs`
Expected: PASS，8 个测试全绿

- [ ] **Step 5: 提交**

```bash
cd ~/印尼语学习
git add lib/provider.js tools/provider.test.mjs
git commit -m "feat: 内容访问接口与解锁凭据管理"
```

---

### Task 6: 发音封装 `lib/tts.js`

**Files:**
- Create: `lib/tts.js`
- Test: `tools/tts.test.mjs`

**Interfaces:**
- Consumes: 无
- Produces:
  - `createTts({ synth, VoiceUtterance }): { speak(text), speakSequence(texts, gapMs), stop(), hasIndonesianVoice(): boolean }`
  - `pickIndonesianVoice(voices: Array<{lang: string}>): object | null` — 优先精确匹配 `id-ID`，其次前缀 `id`

- [ ] **Step 1: 写失败的测试**

```js
// tools/tts.test.mjs
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { createTts, pickIndonesianVoice } from '../lib/tts.js';

test('优先选 id-ID 精确匹配', () => {
  const v = pickIndonesianVoice([
    { lang: 'en-US', name: 'A' },
    { lang: 'id', name: 'B' },
    { lang: 'id-ID', name: 'C' },
  ]);
  assert.equal(v.name, 'C');
});

test('没有精确匹配时退回 id 前缀', () => {
  const v = pickIndonesianVoice([{ lang: 'en-US' }, { lang: 'id_ID' }]);
  assert.equal(v.lang, 'id_ID');
});

test('完全没有印尼语音色返回 null', () => {
  assert.equal(pickIndonesianVoice([{ lang: 'en-US' }, { lang: 'zh-CN' }]), null);
});

test('speak 会设置 lang 并调用 synth.speak', () => {
  const spoken = [];
  class FakeUtterance {
    constructor(text) { this.text = text; }
  }
  const synth = {
    getVoices: () => [{ lang: 'id-ID', name: 'Damayanti' }],
    speak: (u) => spoken.push(u),
    cancel: () => {},
  };
  const tts = createTts({ synth, VoiceUtterance: FakeUtterance });
  tts.speak('Selamat pagi');
  assert.equal(spoken.length, 1);
  assert.equal(spoken[0].text, 'Selamat pagi');
  assert.equal(spoken[0].lang, 'id-ID');
  assert.equal(spoken[0].voice.name, 'Damayanti');
});

test('无印尼语音色时仍设 lang，voice 为 undefined', () => {
  const spoken = [];
  class FakeUtterance { constructor(text) { this.text = text; } }
  const synth = { getVoices: () => [{ lang: 'en-US' }], speak: (u) => spoken.push(u), cancel: () => {} };
  const tts = createTts({ synth, VoiceUtterance: FakeUtterance });
  assert.equal(tts.hasIndonesianVoice(), false);
  tts.speak('halo');
  assert.equal(spoken[0].lang, 'id-ID');
  assert.equal(spoken[0].voice, undefined);
});

test('stop 调用 synth.cancel', () => {
  let cancelled = 0;
  class FakeUtterance { constructor(text) { this.text = text; } }
  const synth = { getVoices: () => [], speak: () => {}, cancel: () => { cancelled++; } };
  createTts({ synth, VoiceUtterance: FakeUtterance }).stop();
  assert.equal(cancelled, 1);
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd ~/印尼语学习 && node --test tools/tts.test.mjs`
Expected: FAIL — 模块不存在

- [ ] **Step 3: 实现**

```js
// lib/tts.js
// Web Speech API 封装。印尼语音色在各平台可用性不同：
// iOS Safari 自带 id-ID（Damayanti）；Android 需装 Google TTS 印尼语包。
// 没有音色时仍设置 lang，让系统尽力朗读，同时由 hasIndonesianVoice() 供 UI 提示。

export function pickIndonesianVoice(voices) {
  const list = voices ?? [];
  const norm = (l) => String(l ?? '').replace('_', '-').toLowerCase();
  return (
    list.find((v) => norm(v.lang) === 'id-id') ??
    list.find((v) => norm(v.lang).split('-')[0] === 'id') ??
    null
  );
}

export function createTts({ synth, VoiceUtterance }) {
  function voice() {
    return pickIndonesianVoice(synth.getVoices());
  }

  function speak(text) {
    if (!text) return;
    const u = new VoiceUtterance(text);
    u.lang = 'id-ID';
    u.rate = 0.9;
    const v = voice();
    if (v) u.voice = v;
    synth.speak(u);
  }

  function speakSequence(texts, gapMs = 400) {
    stop();
    let i = 0;
    const next = () => {
      if (i >= texts.length) return;
      const u = new VoiceUtterance(texts[i++]);
      u.lang = 'id-ID';
      u.rate = 0.9;
      const v = voice();
      if (v) u.voice = v;
      u.onend = () => setTimeout(next, gapMs);
      synth.speak(u);
    };
    next();
  }

  return {
    speak,
    speakSequence,
    stop: () => synth.cancel(),
    hasIndonesianVoice: () => voice() !== null,
  };
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd ~/印尼语学习 && node --test tools/tts.test.mjs`
Expected: PASS，6 个测试全绿

- [ ] **Step 5: 提交**

```bash
cd ~/印尼语学习
git add lib/tts.js tools/tts.test.mjs
git commit -m "feat: 印尼语发音封装"
```

---

### Task 7: 配图映射 `lib/icons.js` 与 `tools/fetch-openmoji.mjs`

**Files:**
- Create: `lib/icons.js`、`lib/emoji-map.js`、`tools/fetch-openmoji.mjs`
- Test: `tools/icons.test.mjs`

**Interfaces:**
- Consumes: 无
- Produces:
  - `lib/emoji-map.js` 导出 `WORD_EMOJI: Record<string, string>`（印尼语词 → OpenMoji 码位，如 `apel: '1F34E'`）与 `THEME_EMOJI: Record<string, string>`（主题名 → 码位）
  - `lib/icons.js` 导出 `iconFor(word: string, themeTitle: string): string` — 返回 SVG 相对路径 `assets/openmoji/<码位>.svg`；词无映射时退回主题图标；主题也无映射时退回 `THEME_EMOJI.默认`
  - `codepointsInUse(packs): string[]` — 供抓取脚本收集需要下载的码位

- [ ] **Step 1: 写失败的测试**

```js
// tools/icons.test.mjs
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { iconFor, codepointsInUse } from '../lib/icons.js';
import { WORD_EMOJI, THEME_EMOJI } from '../lib/emoji-map.js';

test('词有映射时用词的图', () => {
  assert.equal(iconFor('apel', '水果'), `assets/openmoji/${WORD_EMOJI.apel}.svg`);
});

test('词无映射时退回主题图', () => {
  assert.equal(
    iconFor('kata-yang-tidak-ada', '水果'),
    `assets/openmoji/${THEME_EMOJI['水果']}.svg`,
  );
});

test('词与主题都无映射时退回默认图', () => {
  assert.equal(
    iconFor('zzz', '不存在的主题'),
    `assets/openmoji/${THEME_EMOJI['默认']}.svg`,
  );
});

test('大小写与空白不影响匹配', () => {
  assert.equal(iconFor('  APEL ', '水果'), `assets/openmoji/${WORD_EMOJI.apel}.svg`);
});

test('codepointsInUse 收集词与主题两类码位且去重', () => {
  const packs = [
    { title: '水果', words: [{ word: 'apel' }, { word: 'zzz-unknown' }] },
    { title: '水果', words: [{ word: 'apel' }] },
  ];
  const cps = codepointsInUse(packs);
  assert.ok(cps.includes(WORD_EMOJI.apel));
  assert.ok(cps.includes(THEME_EMOJI['水果']));
  assert.ok(cps.includes(THEME_EMOJI['默认']));
  assert.equal(new Set(cps).size, cps.length);
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd ~/印尼语学习 && node --test tools/icons.test.mjs`
Expected: FAIL — 模块不存在

- [ ] **Step 3: 实现**

`lib/emoji-map.js` 的完整映射在实施时按实际词表生成，这里给出结构与必需的种子条目（`apel`、`水果`、`默认` 是测试依赖项，必须存在）：

```js
// lib/emoji-map.js
// 印尼语词 → OpenMoji 码位。键一律小写、去空白。
// 覆盖不全是预期的：抽象词没有对应图形，由 THEME_EMOJI 兜底。
export const WORD_EMOJI = {
  apel: '1F34E',
  pisang: '1F34C',
  jeruk: '1F34A',
  anggur: '1F347',
  semangka: '1F349',
  // …实施时按 content-src/packs.json 的实际词表补全
};

// 主题名（单词包 title）→ OpenMoji 码位。'默认' 必须存在，作为最终兜底。
export const THEME_EMOJI = {
  水果: '1F34E',
  蔬菜: '1F966',
  数字: '1F522',
  颜色: '1F3A8',
  身体: '1F9CD',
  主食: '1F35A',
  饮料: '1F964',
  房间: '1F3E0',
  家具: '1F6CB',
  包装: '1F4E6',
  海鲜: '1F41F',
  肉蛋豆: '1F95A',
  默认: '1F4D6',
};
```

```js
// lib/icons.js
// 词 → 配图路径。三级回退：词映射 → 主题映射 → 默认图。
import { WORD_EMOJI, THEME_EMOJI } from './emoji-map.js';

const DIR = 'assets/openmoji';
const FALLBACK = '默认';

function key(s) {
  return String(s ?? '').trim().toLowerCase();
}

export function codepointFor(word, themeTitle) {
  return (
    WORD_EMOJI[key(word)] ??
    THEME_EMOJI[String(themeTitle ?? '').trim()] ??
    THEME_EMOJI[FALLBACK]
  );
}

export function iconFor(word, themeTitle) {
  return `${DIR}/${codepointFor(word, themeTitle)}.svg`;
}

export function codepointsInUse(packs) {
  const set = new Set([THEME_EMOJI[FALLBACK]]);
  for (const pack of packs ?? []) {
    const theme = THEME_EMOJI[String(pack.title ?? '').trim()];
    if (theme) set.add(theme);
    for (const w of pack.words ?? []) {
      const cp = WORD_EMOJI[key(w.word)];
      if (cp) set.add(cp);
    }
  }
  return [...set];
}
```

```js
// tools/fetch-openmoji.mjs
// 按映射表从 jsdelivr 拉取用到的 OpenMoji SVG 子集。
// 不安装 npm 全量包（几百 MB），只下载实际引用到的码位。
//
// OpenMoji 17.0.0，授权 CC-BY-SA-4.0 —— 应用内必须署名。
import { readFileSync, writeFileSync, mkdirSync, existsSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { dirname, join } from 'node:path';
import { codepointsInUse } from '../lib/icons.js';

const CDN = 'https://cdn.jsdelivr.net/npm/openmoji@17.0.0/color/svg';

const root = join(dirname(fileURLToPath(import.meta.url)), '..');
const outDir = join(root, 'assets/openmoji');
mkdirSync(outDir, { recursive: true });

const packs = JSON.parse(
  readFileSync(join(root, 'content-src/packs.json'), 'utf8'),
);
const cps = codepointsInUse(packs);

let downloaded = 0;
let skipped = 0;
const missing = [];

for (const cp of cps) {
  const dest = join(outDir, `${cp}.svg`);
  if (existsSync(dest)) { skipped++; continue; }
  const res = await fetch(`${CDN}/${cp}.svg`);
  if (!res.ok) { missing.push(cp); continue; }
  writeFileSync(dest, await res.text());
  downloaded++;
}

console.log(`OpenMoji：新下载 ${downloaded}，已存在 ${skipped}，缺失 ${missing.length}`);
if (missing.length) console.log('缺失码位：', missing.join(', '));
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd ~/印尼语学习 && node --test tools/icons.test.mjs`
Expected: PASS，5 个测试全绿

- [ ] **Step 5: 提交**

```bash
cd ~/印尼语学习
git add lib/icons.js lib/emoji-map.js tools/fetch-openmoji.mjs tools/icons.test.mjs
git commit -m "feat: OpenMoji 配图映射与抓取脚本"
```

---

### Task 8: 生成内容 — 1000 词条

按 `content-src/pack-skeleton.json` 的 100 个主题逐包填词，每包 10 条。分 10 批，每批 10 个包。

**Files:**
- Create: `content-src/packs.json`（gitignored）
- Create: `tools/check-content.mjs`
- Test: `tools/check-content.test.mjs`

**Interfaces:**
- Consumes: `content-src/pack-skeleton.json`
- Produces:
  - `validatePacks(packs): string[]` — 返回问题描述数组，空数组表示通过

- [ ] **Step 1: 写失败的测试**

```js
// tools/check-content.test.mjs
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { validatePacks } from './check-content.mjs';

const good = [{
  id: 'freq-beginner-001', title: '数字', subtitle: '1到10', theme: 'blue', stage: 1,
  words: Array.from({ length: 10 }, (_, i) => ({
    id: `freq-beginner-001-${i + 1}`,
    word: `kata${i}`, pos: '名词', zh: '词',
    example: `Ini kata${i}.`, exampleZh: '这是词。',
  })),
}];

test('合格内容无问题', () => {
  assert.deepEqual(validatePacks(good), []);
});

test('词数不等于 10 报错', () => {
  const bad = structuredClone(good);
  bad[0].words.pop();
  assert.match(validatePacks(bad)[0], /freq-beginner-001.*9 词/);
});

test('缺字段报错', () => {
  const bad = structuredClone(good);
  delete bad[0].words[3].exampleZh;
  assert.match(validatePacks(bad)[0], /exampleZh/);
});

test('包内重复词报错', () => {
  const bad = structuredClone(good);
  bad[0].words[1].word = bad[0].words[0].word;
  assert.match(validatePacks(bad)[0], /重复/);
});

test('例句必须包含该词', () => {
  const bad = structuredClone(good);
  bad[0].words[0].example = 'Kalimat lain.';
  assert.match(validatePacks(bad)[0], /例句未包含/);
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd ~/印尼语学习 && node --test tools/check-content.test.mjs`
Expected: FAIL — 模块不存在

- [ ] **Step 3: 实现校验脚本**

```js
// tools/check-content.mjs
// 词库校验。生成内容后必须跑，防止批量生成时字段漏填或例句跑题。
import { readFileSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { dirname, join } from 'node:path';

const REQUIRED = ['id', 'word', 'pos', 'zh', 'example', 'exampleZh'];

export function validatePacks(packs) {
  const problems = [];
  const seenPackIds = new Set();

  for (const pack of packs ?? []) {
    if (seenPackIds.has(pack.id)) problems.push(`包 ${pack.id} 重复出现`);
    seenPackIds.add(pack.id);

    const words = pack.words ?? [];
    if (words.length !== 10) {
      problems.push(`包 ${pack.id}（${pack.title}）有 ${words.length} 词，应为 10 词`);
    }

    const seenWords = new Set();
    for (const w of words) {
      for (const f of REQUIRED) {
        if (!w?.[f]) problems.push(`包 ${pack.id} 的词 ${w?.word ?? '?'} 缺字段 ${f}`);
      }
      const key = String(w?.word ?? '').toLowerCase();
      if (seenWords.has(key)) problems.push(`包 ${pack.id} 内重复词：${w.word}`);
      seenWords.add(key);

      if (w?.word && w?.example) {
        const stem = String(w.word).toLowerCase().slice(0, Math.max(3, w.word.length - 2));
        if (!String(w.example).toLowerCase().includes(stem)) {
          problems.push(`包 ${pack.id} 的词 ${w.word} 例句未包含该词：${w.example}`);
        }
      }
    }
  }
  return problems;
}

const isMain = process.argv[1] === fileURLToPath(import.meta.url);
if (isMain) {
  const root = join(dirname(fileURLToPath(import.meta.url)), '..');
  const packs = JSON.parse(readFileSync(join(root, 'content-src/packs.json'), 'utf8'));
  const problems = validatePacks(packs);
  const words = packs.reduce((n, p) => n + (p.words?.length ?? 0), 0);
  console.log(`${packs.length} 个包 / ${words} 词`);
  if (problems.length) {
    console.error(`发现 ${problems.length} 个问题：`);
    for (const p of problems.slice(0, 50)) console.error('  -', p);
    process.exit(1);
  }
  console.log('校验通过');
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd ~/印尼语学习 && node --test tools/check-content.test.mjs`
Expected: PASS，5 个测试全绿

- [ ] **Step 5: 逐批生成词条**

以 `content-src/pack-skeleton.json` 为准，分 10 批填词，每批 10 个包 100 词。每条格式：

```json
{
  "id": "freq-beginner-001-1",
  "word": "satu",
  "pos": "数词",
  "zh": "一",
  "example": "Saya punya satu adik.",
  "exampleZh": "我有一个弟弟／妹妹。"
}
```

生成约束：
- 词必须是印尼语标准拼写，非俚语、非缩写
- `pos` 用中文词性：名词／动词／形容词／数词／副词／代词／介词／连词／量词／感叹词
- `zh` 简短释义，多义词用 `；` 分隔，不超过 20 字
- `example` 必须实际包含该词，长度 4–10 词，日常口语
- `exampleZh` 是 `example` 的中文翻译
- 同一包内不得重复词，跨包尽量不重复

每批写入后追加合并进 `content-src/packs.json`，并运行校验：

```bash
cd ~/印尼语学习 && node tools/check-content.mjs
```

- [ ] **Step 6: 全量校验通过后提交工具（内容本身不提交）**

```bash
cd ~/印尼语学习
node tools/check-content.mjs
git add tools/check-content.mjs tools/check-content.test.mjs
git commit -m "feat: 词库校验脚本"
```

---

### Task 9: 生成内容 — 10 组场景对话

**Files:**
- Create: `content-src/dialogs.json`（gitignored）
- Modify: `tools/check-content.mjs`（新增 `validateDialogs`）
- Modify: `tools/check-content.test.mjs`

**Interfaces:**
- Consumes: 无
- Produces: `validateDialogs(dialogs): string[]`

- [ ] **Step 1: 追加失败的测试**

```js
// 追加到 tools/check-content.test.mjs
import { validateDialogs } from './check-content.mjs';

const goodDialog = [{
  id: 'sapaan', scene: 'Sapaan', sceneZh: '打招呼',
  lines: Array.from({ length: 8 }, (_, i) => ({
    speaker: i % 2 === 0 ? 'A' : 'B',
    id_text: `Baris ${i}.`, zh: `第 ${i} 句。`,
  })),
  keyPhrases: [{ id_text: 'Selamat pagi', zh: '早上好' }],
  vocab: [{ word: 'pagi', zh: '早晨' }],
}];

test('合格对话无问题', () => {
  assert.deepEqual(validateDialogs(goodDialog), []);
});

test('轮次少于 8 报错', () => {
  const bad = structuredClone(goodDialog);
  bad[0].lines = bad[0].lines.slice(0, 5);
  assert.match(validateDialogs(bad)[0], /5 轮/);
});

test('说话人必须交替', () => {
  const bad = structuredClone(goodDialog);
  bad[0].lines[1].speaker = 'A';
  assert.match(validateDialogs(bad)[0], /说话人未交替/);
});

test('缺关键句报错', () => {
  const bad = structuredClone(goodDialog);
  bad[0].keyPhrases = [];
  assert.match(validateDialogs(bad)[0], /关键句/);
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd ~/印尼语学习 && node --test tools/check-content.test.mjs`
Expected: FAIL — `validateDialogs` 未导出

- [ ] **Step 3: 实现**

```js
// 追加到 tools/check-content.mjs
export function validateDialogs(dialogs) {
  const problems = [];
  for (const d of dialogs ?? []) {
    const lines = d.lines ?? [];
    if (lines.length < 8 || lines.length > 12) {
      problems.push(`对话 ${d.id}（${d.sceneZh}）有 ${lines.length} 轮，应为 8–12 轮`);
    }
    for (let i = 1; i < lines.length; i++) {
      if (lines[i].speaker === lines[i - 1].speaker) {
        problems.push(`对话 ${d.id} 第 ${i + 1} 行说话人未交替`);
        break;
      }
    }
    for (const [i, l] of lines.entries()) {
      if (!l.id_text) problems.push(`对话 ${d.id} 第 ${i + 1} 行缺 id_text`);
      if (!l.zh) problems.push(`对话 ${d.id} 第 ${i + 1} 行缺 zh`);
    }
    if (!(d.keyPhrases ?? []).length) problems.push(`对话 ${d.id} 缺关键句`);
    if (!(d.vocab ?? []).length) problems.push(`对话 ${d.id} 缺生词`);
  }
  return problems;
}
```

同时把主流程改成两者都校验：

```js
// 替换 check-content.mjs 末尾 isMain 块中的校验部分
if (isMain) {
  const root = join(dirname(fileURLToPath(import.meta.url)), '..');
  const packs = JSON.parse(readFileSync(join(root, 'content-src/packs.json'), 'utf8'));
  const dialogs = JSON.parse(readFileSync(join(root, 'content-src/dialogs.json'), 'utf8'));
  const problems = [...validatePacks(packs), ...validateDialogs(dialogs)];
  const words = packs.reduce((n, p) => n + (p.words?.length ?? 0), 0);
  console.log(`${packs.length} 个包 / ${words} 词 / ${dialogs.length} 组对话`);
  if (problems.length) {
    console.error(`发现 ${problems.length} 个问题：`);
    for (const p of problems.slice(0, 50)) console.error('  -', p);
    process.exit(1);
  }
  console.log('校验通过');
}
```

- [ ] **Step 4: 生成 10 组对话并校验**

场景固定为：打招呼、买东西、点餐、问路、看病、坐车、租房、银行、学校、教会。

每组 8–12 轮，A/B 交替，附 3–5 条关键句与 5–8 个生词。

Run: `cd ~/印尼语学习 && node --test tools/check-content.test.mjs && node tools/check-content.mjs`
Expected: 测试 PASS；校验输出「校验通过」

- [ ] **Step 5: 提交**

```bash
cd ~/印尼语学习
git add tools/check-content.mjs tools/check-content.test.mjs
git commit -m "feat: 场景对话校验"
```

---

### Task 10: 应用外壳与解锁界面

**Files:**
- Create: `index.html`、`styles.css`、`app.js`、`lib/views/unlock.js`
- Create: `manifest.webmanifest`

**Interfaces:**
- Consumes: `lib/provider.js`、`lib/tts.js`
- Produces:
  - `app.js` 导出 `start(root, provider, tts)`
  - `lib/views/unlock.js` 导出 `renderUnlock(root, { onSubmit, error })`

- [ ] **Step 1: 写外壳**

```html
<!-- index.html -->
<!doctype html>
<html lang="zh-CN">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
<meta name="theme-color" content="#f7f9fc">
<title>印尼语学习</title>
<link rel="manifest" href="manifest.webmanifest">
<link rel="stylesheet" href="styles.css">
</head>
<body>
<div id="app"></div>
<script type="module">
  import { createProvider } from './lib/provider.js';
  import { createTts } from './lib/tts.js';
  import { start } from './app.js';

  const provider = createProvider({
    fetchJson: async (path) => {
      const res = await fetch(path, { cache: 'no-store' });
      if (!res.ok) throw new Error(`加载失败：${path}`);
      return res.json();
    },
    storage: localStorage,
  });

  const tts = createTts({
    synth: window.speechSynthesis,
    VoiceUtterance: window.SpeechSynthesisUtterance,
  });

  start(document.getElementById('app'), provider, tts);

  if ('serviceWorker' in navigator) {
    navigator.serviceWorker.register('sw.js').catch(() => {});
  }
</script>
</body>
</html>
```

```json
{
  "name": "印尼语学习",
  "short_name": "印尼语",
  "start_url": ".",
  "display": "standalone",
  "background_color": "#f7f9fc",
  "theme_color": "#f7f9fc",
  "lang": "zh-CN",
  "icons": [
    { "src": "assets/openmoji/1F4D6.svg", "sizes": "any", "type": "image/svg+xml" }
  ]
}
```

- [ ] **Step 2: 写解锁界面**

```js
// lib/views/unlock.js
export function renderUnlock(root, { onSubmit, error = '' }) {
  root.innerHTML = `
    <section class="unlock">
      <h1>印尼语学习</h1>
      <p class="unlock-hint">输入密码解锁内容</p>
      <form class="unlock-form">
        <input type="password" name="password" autocomplete="current-password"
               placeholder="密码" required>
        <button type="submit">解锁</button>
      </form>
      <p class="unlock-error">${error}</p>
    </section>
  `;
  const form = root.querySelector('.unlock-form');
  form.addEventListener('submit', (e) => {
    e.preventDefault();
    onSubmit(new FormData(form).get('password'));
  });
  root.querySelector('input')?.focus();
}
```

- [ ] **Step 3: 写路由**

```js
// app.js
import { renderUnlock } from './lib/views/unlock.js';
import { renderPackList, renderPack } from './lib/views/packs.js';
import { renderDialogList, renderDialog } from './lib/views/dialogs.js';
import { renderGrammarList, renderGrammarModule } from './lib/views/grammar.js';

const TABS = [
  { id: 'packs', label: '单词包' },
  { id: 'dialogs', label: '场景对话' },
  { id: 'grammar', label: '语法' },
];

export function start(root, provider, tts) {
  let state = { tab: 'packs', detail: null };

  async function boot() {
    const { unlocked } = await provider.init();
    if (unlocked) return renderApp();
    renderUnlock(root, {
      onSubmit: async (password) => {
        try {
          await provider.unlock(password);
          renderApp();
        } catch (err) {
          renderUnlock(root, { onSubmit: arguments.callee, error: err.message });
        }
      },
    });
  }

  function shell(inner) {
    root.innerHTML = `
      <header class="topbar"><h1>印尼语学习</h1></header>
      <main id="main"></main>
      <nav class="tabbar">
        ${TABS.map(
          (t) => `<button data-tab="${t.id}" class="${t.id === state.tab ? 'active' : ''}">${t.label}</button>`,
        ).join('')}
      </nav>
    `;
    root.querySelectorAll('[data-tab]').forEach((b) =>
      b.addEventListener('click', () => {
        state = { tab: b.dataset.tab, detail: null };
        renderApp();
      }),
    );
    root.querySelector('#main').append(inner);
  }

  async function renderApp() {
    const main = document.createElement('div');
    shell(main);
    const open = (detail) => { state = { ...state, detail }; renderApp(); };
    const back = () => { state = { ...state, detail: null }; renderApp(); };

    if (state.tab === 'packs') {
      const packs = await provider.getPacks();
      if (state.detail) renderPack(main, packs.find((p) => p.id === state.detail), { tts, back });
      else renderPackList(main, packs, { open });
    } else if (state.tab === 'dialogs') {
      const dialogs = await provider.getDialogs();
      if (state.detail) renderDialog(main, dialogs.find((d) => d.id === state.detail), { tts, back });
      else renderDialogList(main, dialogs, { open });
    } else {
      const grammar = await provider.getGrammar();
      if (state.detail) renderGrammarModule(main, grammar.find((g) => g.id === state.detail), { tts, back });
      else renderGrammarList(main, grammar, { open });
    }
  }

  boot();
}
```

注意：上面 `arguments.callee` 在严格模式的模块中不可用，实现时必须改成具名函数：

```js
  function showUnlock(error = '') {
    renderUnlock(root, {
      error,
      onSubmit: async (password) => {
        try {
          await provider.unlock(password);
          renderApp();
        } catch (err) {
          showUnlock(err.message);
        }
      },
    });
  }

  async function boot() {
    const { unlocked } = await provider.init();
    if (unlocked) renderApp();
    else showUnlock();
  }
```

- [ ] **Step 4: 本地起服务验证**

```bash
cd ~/印尼语学习 && python3 -m http.server 8123
```

浏览器打开 `http://localhost:8123`，应看到密码输入框。

- [ ] **Step 5: 提交**

```bash
cd ~/印尼语学习
git add index.html styles.css app.js lib/views/unlock.js manifest.webmanifest
git commit -m "feat: 应用外壳与解锁界面"
```

---

### Task 11: 单词包视图 `lib/views/packs.js`

**Files:**
- Create: `lib/views/packs.js`
- Modify: `styles.css`

**Interfaces:**
- Consumes: `lib/icons.js` 的 `iconFor`
- Produces:
  - `renderPackList(root, packs, { open })`
  - `renderPack(root, pack, { tts, back })`

- [ ] **Step 1: 实现列表与词卡**

```js
// lib/views/packs.js
// 单词包：网格列表 + 词卡。不显示任何进度、不记录任何状态。
import { iconFor } from '../icons.js';

export function renderPackList(root, packs, { open }) {
  root.innerHTML = `
    <div class="pack-grid">
      ${packs
        .map(
          (p) => `
        <button class="pack-card" data-id="${p.id}">
          <img class="pack-icon" src="${iconFor('', p.title)}" alt="" loading="lazy">
          <span class="pack-title">${p.title}</span>
          <span class="pack-subtitle">${p.subtitle}</span>
          <span class="pack-count">${p.words.length} 词</span>
        </button>`,
        )
        .join('')}
    </div>`;
  root.querySelectorAll('.pack-card').forEach((el) =>
    el.addEventListener('click', () => open(el.dataset.id)),
  );
}

export function renderPack(root, pack, { tts, back }) {
  let index = 0;
  let flipped = false;

  function draw() {
    const w = pack.words[index];
    root.innerHTML = `
      <div class="card-view">
        <div class="card-head">
          <button class="back">返回</button>
          <span class="card-pos-label">${pack.title} · ${index + 1}/${pack.words.length}</span>
        </div>
        <div class="word-card ${flipped ? 'flipped' : ''}">
          ${
            flipped
              ? `<div class="card-back">
                   <div class="card-zh">${w.zh}</div>
                   <div class="card-pos">${w.pos}</div>
                   <div class="card-example">${w.example}</div>
                   <div class="card-example-zh">${w.exampleZh}</div>
                 </div>`
              : `<div class="card-front">
                   <img class="card-icon" src="${iconFor(w.word, pack.title)}" alt="" loading="lazy">
                   <div class="card-word">${w.word}</div>
                 </div>`
          }
        </div>
        <div class="card-actions">
          <button class="prev" ${index === 0 ? 'disabled' : ''}>上一个</button>
          <button class="speak">🔊 朗读</button>
          <button class="next" ${index === pack.words.length - 1 ? 'disabled' : ''}>下一个</button>
        </div>
      </div>`;

    root.querySelector('.back').addEventListener('click', back);
    root.querySelector('.word-card').addEventListener('click', () => {
      flipped = !flipped;
      draw();
    });
    root.querySelector('.speak').addEventListener('click', (e) => {
      e.stopPropagation();
      tts.speak(flipped ? w.example : w.word);
    });
    root.querySelector('.prev').addEventListener('click', () => {
      if (index > 0) { index--; flipped = false; draw(); }
    });
    root.querySelector('.next').addEventListener('click', () => {
      if (index < pack.words.length - 1) { index++; flipped = false; draw(); }
    });
  }

  draw();
}
```

- [ ] **Step 2: 浏览器验证**

起本地服务，解锁后进入单词包，确认：网格显示、点进包能翻面、朗读按钮出声、上一个/下一个可用、返回可回列表。

- [ ] **Step 3: 提交**

```bash
cd ~/印尼语学习
git add lib/views/packs.js styles.css
git commit -m "feat: 单词包视图"
```

---

### Task 12: 场景对话视图 `lib/views/dialogs.js`

**Files:**
- Create: `lib/views/dialogs.js`
- Modify: `styles.css`

**Interfaces:**
- Produces: `renderDialogList(root, dialogs, { open })`、`renderDialog(root, dialog, { tts, back })`

- [ ] **Step 1: 实现**

```js
// lib/views/dialogs.js
export function renderDialogList(root, dialogs, { open }) {
  root.innerHTML = `
    <ul class="dialog-list">
      ${dialogs
        .map(
          (d) => `
        <li><button class="dialog-item" data-id="${d.id}">
          <span class="dialog-zh">${d.sceneZh}</span>
          <span class="dialog-id">${d.scene}</span>
          <span class="dialog-count">${d.lines.length} 轮</span>
        </button></li>`,
        )
        .join('')}
    </ul>`;
  root.querySelectorAll('.dialog-item').forEach((el) =>
    el.addEventListener('click', () => open(el.dataset.id)),
  );
}

export function renderDialog(root, dialog, { tts, back }) {
  root.innerHTML = `
    <div class="dialog-view">
      <div class="card-head">
        <button class="back">返回</button>
        <span>${dialog.sceneZh}</span>
        <button class="play-all">▶ 连续播放</button>
      </div>
      <ol class="lines">
        ${dialog.lines
          .map(
            (l, i) => `
          <li class="line speaker-${l.speaker}">
            <span class="who">${l.speaker}</span>
            <span class="line-id">${l.id_text}</span>
            <span class="line-zh">${l.zh}</span>
            <button class="speak-line" data-i="${i}">🔊</button>
          </li>`,
          )
          .join('')}
      </ol>
      <h3>关键句</h3>
      <ul class="key-phrases">
        ${dialog.keyPhrases.map((k) => `<li><b>${k.id_text}</b> — ${k.zh}</li>`).join('')}
      </ul>
      <h3>生词</h3>
      <ul class="dialog-vocab">
        ${dialog.vocab.map((v) => `<li><b>${v.word}</b> — ${v.zh}</li>`).join('')}
      </ul>
    </div>`;

  root.querySelector('.back').addEventListener('click', () => { tts.stop(); back(); });
  root.querySelector('.play-all').addEventListener('click', () =>
    tts.speakSequence(dialog.lines.map((l) => l.id_text)),
  );
  root.querySelectorAll('.speak-line').forEach((b) =>
    b.addEventListener('click', () => tts.speak(dialog.lines[Number(b.dataset.i)].id_text)),
  );
}
```

- [ ] **Step 2: 浏览器验证**

进入场景对话，确认逐行朗读与连续播放均可用，返回时停止朗读。

- [ ] **Step 3: 提交**

```bash
cd ~/印尼语学习
git add lib/views/dialogs.js styles.css
git commit -m "feat: 场景对话视图"
```

---

### Task 13: 语法视图 `lib/views/grammar.js`

**Files:**
- Create: `lib/views/grammar.js`
- Modify: `styles.css`

**Interfaces:**
- Produces: `renderGrammarList(root, modules, { open })`、`renderGrammarModule(root, module, { tts, back })`

- [ ] **Step 1: 实现**

```js
// lib/views/grammar.js
export function renderGrammarList(root, modules, { open }) {
  root.innerHTML = `
    <ul class="grammar-list">
      ${modules
        .map(
          (m) => `
        <li><button class="grammar-item" data-id="${m.id}">
          <span class="grammar-number">${m.number}</span>
          <span class="grammar-title">${m.title}</span>
          <span class="grammar-subtitle">${m.subtitle}</span>
          <span class="grammar-count">${m.lessons.length} 课</span>
        </button></li>`,
        )
        .join('')}
    </ul>`;
  root.querySelectorAll('.grammar-item').forEach((el) =>
    el.addEventListener('click', () => open(el.dataset.id)),
  );
}

export function renderGrammarModule(root, mod, { tts, back }) {
  root.innerHTML = `
    <div class="grammar-view">
      <div class="card-head">
        <button class="back">返回</button>
        <span>${mod.title}</span>
      </div>
      ${mod.lessons
        .map(
          (l) => `
        <section class="lesson">
          <h3>${l.title}</h3>
          <p class="lesson-base">${l.base}</p>
          <p class="lesson-instruction">${l.instruction}</p>
          <ul class="options">
            ${l.options
              .map(
                (o) => `
              <li class="option">
                <span class="option-label">${o.label}</span>
                <span class="option-result">${o.result}</span>
                <span class="option-meaning">${o.meaning}</span>
                <span class="option-example">${o.example}</span>
                <span class="option-translation">${o.translation}</span>
                ${o.note ? `<span class="option-note">${o.note}</span>` : ''}
                <button class="speak-option" data-text="${o.example}">🔊</button>
              </li>`,
              )
              .join('')}
          </ul>
        </section>`,
        )
        .join('')}
    </div>`;

  root.querySelector('.back').addEventListener('click', () => { tts.stop(); back(); });
  root.querySelectorAll('.speak-option').forEach((b) =>
    b.addEventListener('click', () => tts.speak(b.dataset.text)),
  );
}
```

- [ ] **Step 2: 浏览器验证**

进入语法标签，确认模块列表与课程详情渲染正常，例句可朗读。

- [ ] **Step 3: 提交**

```bash
cd ~/印尼语学习
git add lib/views/grammar.js styles.css
git commit -m "feat: 语法学习视图"
```

---

### Task 14: Service Worker 与离线

**Files:**
- Create: `sw.js`
- Test: `tools/sw-strategy.test.mjs`

**Interfaces:**
- Produces: `chooseStrategy(pathname): 'network-first' | 'cache-first'`（从 `sw.js` 导出供测试；`sw.js` 内部同时使用）

- [ ] **Step 1: 写失败的测试**

```js
// tools/sw-strategy.test.mjs
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { chooseStrategy } from '../sw.js';

test('manifest 与 keys 走 network-first', () => {
  assert.equal(chooseStrategy('/data/manifest.json'), 'network-first');
  assert.equal(chooseStrategy('/data/keys.json'), 'network-first');
});

test('带版本号的加密内容走 cache-first', () => {
  assert.equal(chooseStrategy('/data/v1/packs.enc'), 'cache-first');
  assert.equal(chooseStrategy('/data/v2/grammar.enc'), 'cache-first');
});

test('外壳资源走 cache-first', () => {
  assert.equal(chooseStrategy('/index.html'), 'cache-first');
  assert.equal(chooseStrategy('/lib/provider.js'), 'cache-first');
  assert.equal(chooseStrategy('/assets/openmoji/1F34E.svg'), 'cache-first');
});

test('子路径部署（GitHub Pages）下依然正确', () => {
  assert.equal(chooseStrategy('/印尼语学习/data/keys.json'), 'network-first');
  assert.equal(chooseStrategy('/印尼语学习/data/v1/packs.enc'), 'cache-first');
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd ~/印尼语学习 && node --test tools/sw-strategy.test.mjs`
Expected: FAIL — `sw.js` 不存在

- [ ] **Step 3: 实现**

```js
// sw.js
// 缓存策略是密码轮换能否生效的关键：
// manifest.json / keys.json 必须 network-first，否则用户的 PWA 会一直
// 用着旧的加密包和旧的 keys，换了密码也没有感觉。
// data/<version>/*.enc 的 URL 带版本号，内容不可变，cache-first 安全。

const CACHE = 'indo-learn-v1';
const SHELL = [
  './',
  './index.html',
  './styles.css',
  './app.js',
  './manifest.webmanifest',
  './lib/crypto.js',
  './lib/provider.js',
  './lib/tts.js',
  './lib/icons.js',
  './lib/emoji-map.js',
  './lib/views/unlock.js',
  './lib/views/packs.js',
  './lib/views/dialogs.js',
  './lib/views/grammar.js',
];

export function chooseStrategy(pathname) {
  if (/\/data\/(manifest|keys)\.json$/.test(pathname)) return 'network-first';
  return 'cache-first';
}

async function cacheFirst(request) {
  const hit = await caches.match(request);
  if (hit) return hit;
  const res = await fetch(request);
  if (res.ok) (await caches.open(CACHE)).put(request, res.clone());
  return res;
}

async function networkFirst(request) {
  try {
    const res = await fetch(request, { cache: 'no-store' });
    if (res.ok) (await caches.open(CACHE)).put(request, res.clone());
    return res;
  } catch {
    const hit = await caches.match(request);
    if (hit) return hit;
    throw new Error('离线且无缓存');
  }
}

if (typeof self !== 'undefined' && 'addEventListener' in self) {
  self.addEventListener('install', (e) => {
    e.waitUntil(caches.open(CACHE).then((c) => c.addAll(SHELL)).then(() => self.skipWaiting()));
  });

  self.addEventListener('activate', (e) => {
    e.waitUntil(
      caches.keys()
        .then((keys) => Promise.all(keys.filter((k) => k !== CACHE).map((k) => caches.delete(k))))
        .then(() => self.clients.claim()),
    );
  });

  self.addEventListener('fetch', (e) => {
    if (e.request.method !== 'GET') return;
    const strategy = chooseStrategy(new URL(e.request.url).pathname);
    e.respondWith(strategy === 'network-first' ? networkFirst(e.request) : cacheFirst(e.request));
  });
}
```

注意：`index.html` 注册 Service Worker 时必须用 `{ type: 'module' }`，因为 `sw.js` 使用了 `export`：

```js
navigator.serviceWorker.register('sw.js', { type: 'module' }).catch(() => {});
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd ~/印尼语学习 && node --test tools/sw-strategy.test.mjs`
Expected: PASS，4 个测试全绿

- [ ] **Step 5: 提交**

```bash
cd ~/印尼语学习
git add sw.js tools/sw-strategy.test.mjs index.html
git commit -m "feat: Service Worker 离线缓存"
```

---

### Task 15: 打包、署名与部署文档

**Files:**
- Create: `README.md`
- Create: `.github/workflows/pages.yml`
- Modify: `index.html`（加 OpenMoji 署名）

- [ ] **Step 1: 打包内容**

```bash
cd ~/印尼语学习
node tools/extract-packs.mjs
node tools/extract-grammar.mjs
node tools/check-content.mjs
node tools/fetch-openmoji.mjs
node tools/pack-content.mjs --password 'REDACTED' --version v1
```

- [ ] **Step 2: 加 OpenMoji 署名**

CC-BY-SA-4.0 要求署名。在 `index.html` 的 `</body>` 前加：

```html
<footer class="attribution">
  配图来自 <a href="https://openmoji.org" target="_blank" rel="noopener">OpenMoji</a>，
  授权 <a href="https://creativecommons.org/licenses/by-sa/4.0/" target="_blank" rel="noopener">CC BY-SA 4.0</a>
</footer>
```

- [ ] **Step 3: 写 README**

```markdown
# 印尼语学习

纯前端离线 PWA。三个模块：单词包、场景对话、语法学习。内容经 AES-GCM 加密，需密码解锁。

## 换密码

换密码必须同时提高版本号 —— 脚本每次都会生成新的 CEK 并重新加密全部内容：

    node tools/pack-content.mjs --password '新密码' --version v2

只换 keys.json 而复用 CEK 是无效的：保留了旧 keys.json 的人用旧密码仍能解出同一个 CEK。

**当前打包用的是占位密码 `REDACTED`，上线前必须换掉。**

## 安全边界

纯静态离线应用做不到真正的吊销。用户一旦下载过加密包并拿到过密码，那份数据就永久
在其手中，更换密码不影响已持有的副本。密码轮换只保护此后发布的内容。离线状态下应用
会回落到缓存的旧 manifest 与 keys，此时旧密码仍然有效。要做到即时吊销只能上服务器。

## 本地开发

    python3 -m http.server 8123

## 跑测试

    node --test tools/*.test.mjs

## 重新生成内容

    node tools/extract-packs.mjs      # 单词包骨架
    node tools/extract-grammar.mjs    # 语法课程 + SVG
    node tools/check-content.mjs      # 校验词库与对话
    node tools/fetch-openmoji.mjs     # 拉取用到的 OpenMoji SVG
    node tools/pack-content.mjs --password '密码' --version v1

## 授权

配图来自 [OpenMoji](https://openmoji.org)，授权 CC BY-SA 4.0。
```

- [ ] **Step 4: 部署工作流**

```yaml
# .github/workflows/pages.yml
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: .
      - id: deployment
        uses: actions/deploy-pages@v4
```

- [ ] **Step 5: 全量测试并提交**

```bash
cd ~/印尼语学习
node --test tools/*.test.mjs
git add README.md .github/workflows/pages.yml index.html data assets
git commit -m "feat: 打包内容、OpenMoji 署名与 Pages 部署"
```

---

## Self-Review

**Spec coverage**

| 规格要求 | 对应任务 |
|---|---|
| 三模块 PWA | Task 10–13 |
| 无进度记录 | Global Constraints；Task 11 视图不含任何进度元素 |
| 外壳与数据分离 | Task 4、5、10 |
| AES-GCM + PBKDF2 加密 | Task 1、4 |
| 双层密钥（CEK / KEK） | Task 1、4 |
| 密码轮换换 CEK | Task 4 Step 1 最后一个测试 + README |
| 解锁凭据 30 天有效期 | Task 5 |
| 版本变化使凭据失效 | Task 5 |
| 明文不落盘 | Task 5 实现注释 + Global Constraints |
| provider 接口留服务器口子 | Task 5 |
| SW 缓存策略 | Task 14 |
| Web Speech id-ID | Task 6 |
| OpenMoji 子集 + 主题兜底 | Task 7 |
| 提取语法课程与 SVG | Task 3 |
| 提取 100 包骨架 | Task 2 |
| 生成 1000 词条 | Task 8 |
| 生成 10 组对话 | Task 9 |
| GitHub Pages 部署 | Task 15 |
| 不提交明文与密码 | `.gitignore`（已提交）+ Task 15 |
| OpenMoji 署名 | Task 15 |

**Placeholder scan**：Task 7 的 `WORD_EMOJI` 标注了「实施时按实际词表补全」——这是内容映射，依赖 Task 8 产出的词表，无法提前枚举；测试所依赖的种子条目已全部给出。其余无占位。

**Type consistency**：`generateCek` / `deriveKek` / `wrapCek` / `unwrapCek` / `encryptJson` / `decryptJson` / `exportCek` / `importCek` / `randomB64` 在 Task 1 定义，Task 4、5 使用一致。`iconFor` / `codepointsInUse` 在 Task 7 定义，Task 11 与 `tools/fetch-openmoji.mjs` 使用一致。`validatePacks` / `validateDialogs` 在 Task 8、9 一致。`chooseStrategy` 在 Task 14 定义并测试。视图函数 `renderPackList` / `renderPack` / `renderDialogList` / `renderDialog` / `renderGrammarList` / `renderGrammarModule` / `renderUnlock` 与 `app.js` 的调用签名一致。
